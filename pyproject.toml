[tool.poetry]
name = "securityreporter"
version = "0.1.0"
description = ""
authors = ["Alexander Krigsman <alexander.krigsman@dongit.nl>"]
packages = [
    { include = "reporter" },
]

[tool.poetry.dependencies]
python = "^3.7.2"
requests = "^2.27.1"

[tool.poetry.group.dev.dependencies]
pytest = "^7.1.2"
black = "^22.1.0"
mypy = "^0.931"
pytest-docker = "^0.12.0"
docker = "^5.0.3"
docker-compose = "^1.29.2"
responses = "^0.21.0"
types-requests = "^2.28.0"
coverage = "^6.4.2"
# TODO Update to Sphinx 5 when 5.0.3 is released:
# - https://github.com/sphinx-doc/sphinx/pull/10595
Sphinx = "^4.0.0"
pylint = "^2.14.5"
tox = "^3.26.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
